import { Request, Response, NextFunction } from "express";
import { catchAsync } from "../utils/catchAsync";

export const createTag = catchAsync(async () => {});

export const getAllTags = catchAsync(async () => {});

export const getTag = catchAsync(async () => {});

export const updateTag = catchAsync(async () => {});

export const deleteTag = catchAsync(async () => {});
